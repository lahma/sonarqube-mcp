# 1.0.0

Initial release.

- TODO(PhaseE): replace this placeholder with the 1.0.0 release notes naming all nineteen tools.
  The first line of this file is the version authority — the Fallout build parses it in
  `OnBuildInitialized` and a `# Changelog` title would abort the build — so the `# 1.0.0` heading
  above stays exactly as it is.
